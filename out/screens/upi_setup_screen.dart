import 'package:flutter/material.dart';

class UpiSetupScreen extends StatelessWidget {
  const UpiSetupScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
